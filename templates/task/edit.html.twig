{% extends 'base.html.twig' %}

{% block body %}
<div class="d-flex flex-column align-items-center">
    {{ form_start(form, {'action': path('task_edit', { 'id' : task.id }), 'attr': {'class': 'col-6'}}) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success">Modifier</button>
    {{ form_end(form) }}
</div>
{% endblock %}